# Canonical Docker image tags for MySQL/MariaDB binlog integration tests.
# Consumed by: Makefile (via scripts/test-images.mk), CI workflows, and Rust
# test helpers (binlog-protocol::test_images). Override at runtime with env vars.
MYSQL_BINLOG_IMAGE=mysql:8.0
MARIADB_BINLOG_IMAGE=mariadb:11.4

//! SurrealDB connection and write settings (plain fields, no CLI parsing).
//!
//! [`SurrealConfig`] lives in [`surreal_sync_core`]; re-exported here for
//! existing `surreal_sync_runtime::SurrealConfig` imports.

pub use surreal_sync_core::SurrealConfig;

[package]
name = "surreal3-client"
version = "0.1.0"
edition = "2021"
description = "SurrealDB v3 test client implementation"
license = "MIT"

[dependencies]
surreal-client = { path = "../surreal-client" }
surrealdb = { version = "3.0.0-beta.2", features = ["protocol-ws", "kv-mem"] }
anyhow = "1.0.100"
async-trait = "0.1"
tracing = "0.1"
serde_json = "1.0"
